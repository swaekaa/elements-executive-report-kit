- [x] Create Global Dark Theme variables in CSS (or apply directly via inline styles/Tailwind)
- [x] Update `LeftSidebar.tsx` to Dark Theme
- [x] Update `TopToolbar.tsx` to Dark Theme
- [x] Update `RightSidebar.tsx` container to Dark Theme
- [x] Update `PropertyField` in `RightSidebar.tsx` to match the dark theme inputs and typography
- [x] Create `ArrayBuilder` component in `RightSidebar.tsx` for complex arrays
- [x] Implement array items with index prefixes (01, 02), 'Remove' button, and form grids
- [x] Apply `ArrayBuilder` to `timeline`
- [x] Apply `ArrayBuilder` to `metrics`
- [x] Apply `ArrayBuilder` to `financials`/`tables`
- [x] Verify functionality (Add, Edit, Remove) and visual match with reference images
