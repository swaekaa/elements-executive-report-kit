# Phase 4: Dynamic Inspector (Property Editor)

- `[ ]` Create `src/editor/RightSidebar.tsx` dynamic form generator
- `[ ]` Bind the active selected section's properties (via Component Metadata schema) to the inspector inputs
- `[ ]` Allow users to bind variables directly from the property inspector (e.g. typing `{{` opens autocomplete)
- `[ ]` Sync property changes back to the `documentData` object
